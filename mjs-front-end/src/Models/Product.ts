export type Product = {
    name: string,
    code: string,
    category: string,
    price: number
}

export type SoldProduct = {
    name: string,
    code: string,
    amount: number,
    price: number,
    discount: number
}